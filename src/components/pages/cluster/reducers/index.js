import clusterDetail from './detail';
import clusterEdit from './edit';
import clusterPlugins from './plugin';

const reducers = [
  clusterDetail,
  clusterEdit,
  clusterPlugins,
];

export default reducers;
